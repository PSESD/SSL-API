test:
  override:
    - curl --retry 10 --retry-delay 5 -v https://api.cbo.upward.st

deployment:
  push_to_server:
      branch: develop
      commands:
        - git pull cbo@cbo.upward.st:/home/cbo/docker/api
        - chmod +x cbo@cbo.upward.st:/docker.sh
        - cbo@cbo.upward.st:/docker.sh
