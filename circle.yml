test:
  override:
    - curl --retry 10 --retry-delay 5 -v https://api.cbo.upward.st

deployment:
  push_to_server:
      branch: develop
      commands:
        - git pull circleci@104.192.103.13:/home/cbo/docker/api
        - chmod +x circleci@104.192.103.13:/docker.sh
        - circleci@104.192.103.13:/docker.sh
