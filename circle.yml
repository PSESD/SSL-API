test:
  override:
    - curl --retry 10 --retry-delay 5 -v https://api.cbo.upward.st

deployment:
  push_to_server:
      branch: master
      commands:
        - cd /home/cbo/docker/api
        - ./docker.sh
